# Pandas Assignment: Data Exploration & Analysis

## Question 1: IRIS Dataset
Start by loading the IRIS dataset.

**A. Display the following information:**
* First 10 rows
* Shape & Data types
* Summary statistics (mean, std, min, max)

**B. Select only those rows where:**
* `petal_length` > 4.5
* `species` = "Iris-virginica"

**C. Group by species and compute:**
* Average `sepal_length`
* Maximum `petal_width`
* Standard deviation of `sepal_width`

---

## Question 2: Titanic Dataset
Start by loading the TITANIC dataset.

**A. Data Filtering:**
Display only columns: `Name`, `Sex`, `Age`, `Fare`, `Survived`.

**B. Select passengers who:**
* Are female
* `Fare` > 30

**C. Group by Pclass and compute:**
* Survival rate
* Average Fare
* Average Age